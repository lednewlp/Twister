<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render 'twistees/profile' %>
      <% else %>
        <%= render 'twistees/trends' %>
      <% end %>

      <%= render 'twistees/feed' %>
      <%= render 'twistees/who-to-follow' %>
    </div>
  </div>
</section>
